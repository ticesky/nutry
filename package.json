{
  "name": "nut-up",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "license": "ISC",
  "scripts": {
    "build": "tsc -b && pnpm -r --if-present run postbuild",
    "clean": "tsc -b --clean && pnpm -r --if-present run clean:extra",
    "build:clean": "pnpm run clean && pnpm run build",
    "lint": "pnpm run build:clean && pnpm -r --parallel --no-bail run lint",
    "testLint": "pnpm -r --parallel --no-bail run lint"
  },
  "devDependencies": {
    "@babel/core": "^7.28.6",
    "@babel/plugin-transform-typescript": "^7.28.6",
    "@rollup/plugin-babel": "^6.1.0",
    "@types/babel__core": "^7.20.5",
    "@types/caller": "^1.0.2",
    "@types/node": "^20.19.30",
    "@types/ramda": "^0.31.1",
    "@types/resolve": "^1.20.6",
    "eslint": "^9.39.2",
    "rollup": "^4.56.0",
    "stylelint": "^15.10.3",
    "typescript": "next",
    "webpack": "^5.88.2",
    "webpack-dev-server": "^4.15.1",
    "webpack-merge": "^5.9.0"
  },
  "dependencies": {
    "ramda": "^0.32.0"
  }
}
